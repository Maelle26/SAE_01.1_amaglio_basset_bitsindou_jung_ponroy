import java.awt.image.BufferedImage;
import java.awt.Color;
import java.io.File;
import javax.imageio.ImageIO;
import java.util.Random;
import java.util.ArrayList;
import java.util.List;

public class MainTest2 {

    public static void main(String[] args) {
        /*
        try {
            BufferedImage image = ImageIO.read(new File("./projet/src/img.png"));
            int k = 5; // nombre de clusters
            int maxIterations = 100; // nombre maximum d'itérations

            int[][] pixelData = Algo.getPixelData(image);
            KMeans kMeans = new KMeans(k, maxIterations, pixelData);
            int[][] clusteredPixels = kMeans.run();

            BufferedImage outputImage = Algo.createClusteredImage(image, clusteredPixels);
            ImageIO.write(outputImage, "jpg", new File("copie.jpg"));
        } catch (Exception e) {
            e.printStackTrace();
        }

         */
    }
}
